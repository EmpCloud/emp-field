<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Base.Theme.EmpMonitorFieldTrackingUI" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="fontFamily">@font/montserrat_semibold</item>
        <item name="android:fontFamily">@font/montserrat_semibold</item>
        <item name="android:radioButtonStyle">@style/RadioButton</item>
        <!--        <item name="android:switchStyle">@style/SCBSwitchStyle</item>-->

        <!-- Edge-to-edge is handled in code via EdgeToEdgeUtils. On API 35+ the
             status/navigation bar colors are ignored, so only the icon appearance
             is set here. -->
        <item name="android:windowLightStatusBar">true</item>


    </style>
</resources>